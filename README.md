# AI2 Text Factors

Исследовательская реализация разреженной ассоциативной памяти для поиска
повторяющихся факторов в потоке текста.

Проект развивает идеи эксперимента
[«Факторы в тексте»](https://github.com/aldrd/aboutbrain/tree/master/%D0%A4%D0%B0%D0%BA%D1%82%D0%BE%D1%80%D1%8B%20%D0%B2%20%D1%82%D0%B5%D0%BA%D1%81%D1%82%D0%B5),
но написан с нуля на Python. Исходный VB.NET-код и приложенные к нему книжные
корпуса сюда не копируются.

## Что уже работает

- детерминированное разреженное кодирование пар «символ–позиция»;
- латиница и кириллица, настраиваемый односимвольный алфавит через API/CLI;
- несколько циклических контекстных интерпретаций окна;
- случайные локальные рецептивные поля;
- создание, проверка, подрезание и консолидация кластеров памяти;
- режим поиска факторов без учителя;
- экспериментальный режим обучения преобразованию одного контекста в другой;
- объяснение выходного фактора через наиболее связанные символы и позиции;
- безопасное сохранение модели в сжатый NPZ без `pickle`;
- CLI и автоматические тесты.

## v6, блок 2: архив гипотез и возврат

`learned-chat` сохраняет происхождение альтернатив, ищет относящиеся к уточнению
эпизоды и повторно проверяет старые трактовки общей памятью. Дубли не дают новых
оснований; неоднозначный адресат и противоречащие уточнения сохраняют неопределённость.
Поиск использует отдельный обученный линейный ранжировщик и ограниченные бюджеты.
Пересчёт зависимых фактов истории запланирован на блок 3.

[Результаты и ограничения второго блока](reports/v6/02_agreement_attention.md) ·
[Продолжение с третьего блока](reports/v6/RESUME.md).

## v6, блок 1: кандидаты до фиксации фактов

В этой рабочей ветке `learned-chat` сохраняет ограниченный набор трактовок,
сравнивает их с одной общей факторной памятью и только затем выбирает событие.
Неполные структуры и неразрешённые альтернативы не записываются как факты.
Веса v0.5 и формат их загрузки сохраняются; старый API `interpret` доступен
как исходный однозначный разбор, новый API `propose` возвращает кандидатов.

[Результаты и ограничения первого блока](reports/v6/01_candidates_core.md) ·
[Точка продолжения](reports/v6/RESUME.md).

## Новое в v0.5.0a1: обучаемый тракт диалога

Добавлен отдельный `learned-chat`: понимание полной реплики и ролей,
вложенные события и области действия, обучение изменениям ситуации на
факторном ядре, обучаемая политика и условный токенный генератор. Модель
обучается локально без готовой LLM и сохраняет действительные параметры.

```bash
python -m pip install -e .
python -m text_factors learned-train --output learned-model.json
python -m text_factors learned-chat --model learned-model.json --demo
python -m text_factors learned-chat --model learned-model.json --state learned-session.json
```

Это исследовательский диалог в ограниченном предметном мире. Признаки,
онтология ролей и защитные ограничения заданы явно; качество новых
формулировок проверяется отдельно от запоминания учебных примеров.
Команды `learned-export-data`, `learned-freeze` и `learned-evaluate`
предназначены для воспроизводимого обучения и независимой оценки.
Подробнее: [архитектура, запуск и ограничения](docs/V05_LEARNED_DIALOGUE.md),
[протокол проверки](docs/V05_EVALUATION_PROTOCOL.md),
[измеренные результаты](docs/V05_RESULTS.md).

Сохранённая обученная модель включена в репозиторий. Её можно сразу запустить:

```bash
python -m text_factors learned-chat --model docs/results/v05_model_42.json --state learned-session.json
```

Независимая проверка v0.5 выявила слабый перенос языка: точный смысл —
4/17 и 4/19 фраз; сквозной диалог — 24/29 и 0/29 реплик на двух наборах.
Есть смысловые ошибки в утверждениях. Шесть этапов реализованы как
экспериментальная архитектура; качество полноценного чата пока не достигнуто.

## v0.4.0a1: связный диалог с заданной грамматикой

Добавлен отдельный чат `converse`: предметы, места, владельцы, перемещения,
передачи, местоимения, вопросы, исправления, отмена и независимые темы.
Старое ядро и прежний учебный `chat` сохранены.

```bash
python -m pip install -e .
text-factors converse --demo
text-factors converse --state conversation.json
```

Пример: `Я положил ключ в ящик` → `Потом я переложил его в сумку` →
`Где ключ?` → **Ключ в сумке.** Команда `/quit` завершает чат.

Обработка реплики транзакционная; дочерний процесс ограничен по времени,
объёму входа и вывода. Память сохраняется атомарно, а при сбое остаётся
последнее подтверждённое состояние. Сохранённый диалог можно продолжить
той же командой. Новое слово действия задаётся явно: `/teach спрятал move`.

**Граница результата:** это не универсальная разговорная модель. Русская
грамматика, правила ситуации и формулировки ответов заданы программой.
Факторное ядро действительно обучает и распознаёт связь словесного сигнала
с одним из четырёх действий; незнакомые слова не угадываются по шаблону.
Внешняя LLM не используется. Загрузка обычного текста сама по себе не обучает
свободному диалогу.

[Запуск, API и ограничения](docs/V04_GROUNDED_DIALOGUE.md),
[измеренные результаты](docs/V04_RESULTS.md),
[протокол до опыта](docs/V04_DIALOGUE_PROTOCOL.md),
[зафиксированные сценарии](docs/V04_DIALOGUE_CASES.md).

## v0.3.0a4: факторные портреты в целой сцене

- Отдельные предложения содержаний по факторным опорам общей памяти,
  с сохранением перекрытий и необъяснённого остатка.
- Близость контекстов по недавним полным чтениям, включая односторонние
  отклики; повторные ID событий не создают разнообразия примеров.
- Сквозной стенд целых сцен и диагностика влияния окружения на преобразование.

```bash
text-factors scene-integration --output artifacts/scene-integration.json
```

API и ограничения: [V03_SCENE_RECOGNITION.md](docs/V03_SCENE_RECOGNITION.md).
Параметры до опыта: [V03A4_PROTOCOL.md](docs/V03A4_PROTOCOL.md).

## v0.3.0a3: общий путь узнавания

- Обученные SDR-преобразования передают прогнозы в одну память узнавания.
- Близость повторных контекстных откликов учится на отдельных событиях;
  отбор сохраняет разные экземпляры, конфликты и неполные результаты.
- Дополнительный режим названий использует подтверждённые факторные
  свидетельства, сохраняя возможность узнавания при изменении точного ключа.
- Два конечных стенда проверяют сквозную связь и совместную структуру
  при одинаковых частотах битов.

```bash
text-factors context-integration --output artifacts/context-integration.json
text-factors coactivation-structure --output artifacts/joint-structure.json
text-factors chat --demo --grounding factor --state artifacts/factor-chat.json
```

API, ограничения и протокол:
[V03_CONTEXT_INTEGRATION.md](docs/V03_CONTEXT_INTEGRATION.md).
Повторная сверка с источниками, пересчёт уникальных случаев и следующий
архитектурный этап: [V03A3_THEORY_AUDIT.md](docs/V03A3_THEORY_AUDIT.md).

## v0.3.0a2: несколько контекстов и учебный чат

- Добавочное узнавание возвращает несколько результатов с контекстами,
  позициями исходного наблюдения и свидетельствами стабильных кластеров.
- Дубли, совместные вклады, явные противоречия и неопределённые отношения
  различаются; равные выходные биты не означают тождество содержания.
- Отдельный фиксированный стенд проверяет обучение двум SDR-отображениям
  и частичный перенос на новые сочетания, включая отрицательные контроли.
- Собственный учебный чат учит названия по подтверждениям, уточняет
  неопределённость и сохраняет словарь, наблюдения и исправления.

```bash
text-factors chat --demo --state artifacts/chat.json
```

Начните с `покажи ab`, затем `назови 1 куб`. Подробный пример, фактические
результаты и ограничения: [V03_CONTEXT_CHAT.md](docs/V03_CONTEXT_CHAT.md).
Повторная сверка с первоисточниками, причины пропусков и следующий опыт:
[V03A2_THEORY_AUDIT.md](docs/V03A2_THEORY_AUDIT.md).
Команды и шаблоны фраз заданы программой; свободный русский язык и
автоматическое выделение объектов ещё не обучаются.

## Новое в v0.2

- Цифры и ASCII-знаки различаются по умолчанию; пробелы и `_` остаются
  разделителями. Регистр по-прежнему сворачивается.
- Невозможные кодовые книги и повреждённые массивы отклоняются явно.
- Расширение алфавита и обработка потока частями имеют отдельные API.
- NPZ v2 сохраняется атомарно; режимы обучения не смешиваются незаметно.
- Добавлен экспериментальный стенд с базовыми методами, настройкой порога
  только на dev и полными JSON-трассами.
- Отдельный микромир проверяет активный выбор наблюдения из двух заранее
  заданных гипотез. Он **не подключён к SDR как обучаемая модель мира**.

## Экспериментальная v0.3.0a1: факторное ядро и контексты

- Добавлена консолидация по **совместным активациям** с ограниченной историей.
  Частотный режим остаётся исходной точкой сравнения и режимом по умолчанию.
- Повторная консолидация истории не создаёт новые наблюдения и не повышает
  счётчики подтверждений или стадии кластеров.
- Формат NPZ v3 сохраняет историю и дальнейшую траекторию обучения;
  настоящие архивы v1/v2 по-прежнему читаются.
- Новые стенды измеряют локальное восстановление факторов и целевого SDR
  на новых сочетаниях. Они показывают и отрицательные результаты.
- Экспериментальный `ExperienceMemory` соединяет факторную память с реальными
  парами «наблюдение — действие — последствие» и разными представлениями.
  Прогнозы и подтверждённые наблюдения разделены, повторные события не обучают
  память заново. Контексты, кодирование и возможные действия задаёт вызывающий код.

Архитектурные решения: [V03_DESIGN.md](docs/V03_DESIGN.md).
Измеренные результаты и ограничения: [V03_RESULTS.md](docs/V03_RESULTS.md).

Полная пересмотренная дорожная карта до обучающегося агента и чата:
[AI2_TO_CHAT_ROADMAP.md](docs/AI2_TO_CHAT_ROADMAP.md). Она связывает
первоисточники, фактическое состояние кода, недостающие механизмы,
языковые этапы и постепенную реализацию интерфейса разговора.

Первый ограниченный срез этого продолжения реализован в v0.3.0a2:
несколько результатов из общей памяти, попарные отношения и учебный чат.
Автоматическое открытие контекстов и свободный язык остаются дальнейшей работой.
[NEXT_ARCHITECTURE.md](docs/NEXT_ARCHITECTURE.md) описывает контракты наблюдения
и дополнительный эксперимент с ролями и явными правилами. Его
[ручной разбор](docs/ARCHITECTURE_WALKTHROUGH.md) показывает неоднозначность,
дополнительное наблюдение и различающий опыт. Этот стенд не является
обязательным слоем теории; индукция явных программ этого стенда ещё не реализована.

```bash
text-factors train --text "абракадабра абракадабра" --points 128 --consolidation coactivation --history-size 32 --model artifacts/joint.npz
text-factors factor-recovery --output artifacts/factor-recovery.json
text-factors context-transfer --output artifacts/context-transfer.json
text-factors experience-demo --output artifacts/experience.json
```

Размеры экспериментов ограничены; стенды факторов и переноса проверяют бюджет
времени между вызовами и сохраняют статус таймаута. Для полного ограничения
одного зависшего вызова запускайте процесс с внешним таймаутом. Число точек,
кластеров и сохранённых строк определяет расход памяти; ограничение истории
одного кластера не означает небольшой общей памяти при любых настройках.

## Установка

Требуется Python 3.10 или новее.

### Windows PowerShell

```powershell
py -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install -e .
```

### Linux/macOS

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e .
```

## Быстрый запуск

```bash
text-factors demo --text "абракадабра абракадабра абракадабра"
```

Обучение на UTF-8-файле:

```bash
text-factors train \
  --input corpus.txt \
  --model artifacts/model.npz \
  --epochs 3
```

Анализ без изменения модели:

```bash
text-factors analyze \
  --model artifacts/model.npz \
  --text "абракадабра" \
  --top 10
```

Строки длиннее размера кадра автоматически анализируются как последовательность
перекрывающихся окон.

## Python API

```python
from text_factors import ModelConfig, TextFactorModel

config = ModelConfig(point_count=4_000, seed=42)
model = TextFactorModel(config)
model.fit_text("абракадабра абракадабра", epochs=5)

result = model.transform_window("абрак")
print(result.to_dict())

for factor in model.top_factors(5):
    print(factor.to_dict())
    print(model.explain_factor(factor.output_bit, limit=5))

model.save("artifacts/model.npz")
restored = TextFactorModel.load("artifacts/model.npz")
```

## Проверка

Тесты используют стандартную библиотеку и не требуют отдельного тестового
фреймворка:

```bash
python -m unittest discover -s tests -v
python -m compileall -q src tests
```

Полная локальная проверка:

```bash
python -m pip install -e ".[dev]"
ruff check .
ruff format --check .
pyright
```

CI выполняет тесты, форматирование, линтинг и проверку типов на Python 3.10,
3.11 и 3.12.

## Статус проекта

Версия `0.3.0a1` — исследовательский стенд, не СИИ. Стабильный кластер и непустой
выходной код сами по себе не доказывают обнаружение смысла. Проверяемые задачи,
базовые методы и ограничения описаны в [EVALUATION.md](docs/EVALUATION.md).

Исходная v0.1 сохранена в
[коммите eb61a34](https://github.com/anatolahnov5317-hash/AI2/tree/eb61a34a2cb786f1649e0379bc8a7cc7f27a606d).
Сравнивать старые и новые коды напрямую без учёта алфавита/кодовой книги нельзя.

## Воспроизводимые эксперименты

```bash
text-factors evaluate --output artifacts/evaluation.json
text-factors microworld --seed 42 --episodes 32 --action-budget 1 --output artifacts/microworld.json
```

Первый вызов сравнивает факторную память с точным запоминанием, биграммами,
ближайшим SDR и обучаемой парой позиций. Для всех методов используются одни и
те же положительные обучающие строки и число предъявлений. Порог вычисляется
по отрицательным dev-примерам; test не используется для настройки.

Второй вызов — отдельная положительная проверка протокола
«неопределённость → информативное действие → перенос». Объекты и две гипотезы
даны разработчиком; это не открытие новых правил нынешней SDR-памятью.
Подробности: [MICROWORLD.md](docs/MICROWORLD.md).

JSON содержит данные, конфигурацию, хеши исходников, сырые оценки, решения и
метрики. Значения метрик воспроизводимы при тех же версиях зависимостей; время
исполнения зависит от машины. Существующий отчёт не перезаписывается без
`--overwrite`. Первые результаты и дальнейшее решение:
[V02_RESULTS.md](docs/V02_RESULTS.md).

## Старые модели и потоковый ввод

```python
from text_factors import TextFactorModel

model = TextFactorModel.load("artifacts/old-model.npz")
# v1 не хранила режим. Указывать его можно только зная происхождение модели:
model.assume_training_mode("unsupervised")
model.encoder.extend_alphabet("0123456789+-=?!")

for window, offset in model.encoder.iter_chunked_windows(["абра", "кадабра"]):
    model.partial_fit_window(window, offset=offset)
model.save("artifacts/model-v2.npz")
```

Чтение v1 не требует выбора режима. Неизвестный режим блокирует именно
продолжение обучения. Для старой supervised-модели нужно также указать
`source_context` и `target_context`. Одна `TextFactorModel` обучает одну
такую пару. Экспериментальный банк отдельных отображений уже доступен
через [ExperienceMemory](docs/EXPERIENCE.md); сохранение всего банка
и самостоятельное открытие операторов пока не реализованы.

`fit_text()` при каждом вызове обрабатывает отдельный документ. Для одного
потока по кускам используйте `iter_chunked_windows()`; его окна и смещения
совпадают с обработкой склеенного документа.

Подробное описание математики и отличий от прототипа находится в
[`docs/ALGORITHM.md`](docs/ALGORITHM.md).
